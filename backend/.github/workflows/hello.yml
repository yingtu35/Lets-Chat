name: Hello World

on:
  push:
    branches:
      - workflow

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say Hello
        run: echo "Hello World!"
      - name: Date
        run: date
      # - name: List Directory
      #   run: |
      #     ls -l
